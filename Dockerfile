# Используем официальный образ Python как базовый
FROM python:3.11-slim

# Устанавливаем рабочую директорию в контейнере
WORKDIR /app

# Копируем файл зависимостей
# Хотя вы перечислили их, лучше использовать requirements.txt для стандартной практики
# Предполагая, что вы создадите файл requirements.txt со списком зависимостей
COPY requirements.txt .

# Устанавливаем зависимости
RUN pip install --no-cache-dir -r requirements.txt

# Копируем все остальные файлы проекта (включая ваш скрипт bot.py и .env, если он есть)
# Так как у вас все в одной директории, это копирует их все
COPY . .

# Команда для запуска вашего бота
# Замените 'bot.py' на имя вашего основного скрипта, если оно отличается
CMD ["python", "main.py"]